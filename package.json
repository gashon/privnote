{
  "name": "secret-send",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others \"pnpm run db:start\" \"wait-on tcp:5432 && pnpm run dev\"",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "format:check": "prettier --check .",
    "format:write": "prettier --write .",
    "db:start": "docker run -v data:/var/lib/postgresql/data --name secret-send-db -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=secret_send -e POSTGRES_USER=postgres -p 5432:5432 postgres",
    "db:stop": "docker stop secret-send-db && docker rm secret-send-db",
    "db:reset": "docker exec -it secret-send-db psql -U postgres -c 'DROP DATABASE IF EXISTS secret_send'",
    "db:migrate": "node -r esbuild-runner/register  src/db/migrations/migrator.ts",
    "lhci:mobile": "lhci autorun",
    "lhci:desktop": "lhci autorun --collect.settings.preset=desktop"
  },
  "dependencies": {
    "@tanstack/react-query": "^4.29.7",
    "@trpc/client": "^10.27.3",
    "@trpc/react-query": "^10.27.3",
    "@trpc/server": "^10.27.3",
    "@types/uuid": "^9.0.1",
    "autoprefixer": "10.4.14",
    "concurrently": "8.0.1",
    "eslint": "8.41.0",
    "eslint-config-next": "13.4.3",
    "next": "13.4.3",
    "postcss": "8.4.23",
    "prettier": "^2.8.8",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "tailwindcss": "3.3.2",
    "typescript": "5.0.4",
    "uuid": "^9.0.0",
    "wait-on": "7.0.1"
  },
  "devDependencies": {
    "@types/node": "20.2.3",
    "@types/pg": "^8.10.1",
    "@types/react": "18.2.6",
    "@types/react-dom": "18.2.4",
    "esbuild": "^0.17.19",
    "esbuild-runner": "^2.2.2",
    "kysely": "^0.24.2",
    "pg": "^8.11.0"
  }
}
